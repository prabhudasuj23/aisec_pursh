# Trivy CVE ignore list — AISec + Pursh monorepo
#
# HOW THIS WORKS (enterprise model):
#   - Every CVE here has been reviewed by the security team.
#   - Adding a CVE requires a PR approved by a CODEOWNER (security path).
#   - Any CVE NOT listed here will still fail the Security Gate.
#   - Review this file quarterly; remove entries once the base image is patched.
#
# FORMAT:
#   <CVE-ID>  # Package | Severity | Fixed in | Accepted | Reason

# ── python:3.12-slim base image OS packages — accepted 2026-05-19 ──────────
# These CVEs exist in Debian OS packages in the base image only.
# The application code does not invoke these libraries at runtime.
# Fixes exist upstream; remove these entries once python:3.12-slim rebuilds.

CVE-2026-4878
CVE-2026-29111
